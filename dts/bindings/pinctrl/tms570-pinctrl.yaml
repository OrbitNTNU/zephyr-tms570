
description: "Pinctrl for TMS570"
compatible: "tms570,pinctrl"

include: base.yaml

properties:
  reg:
    required: true

child-binding:
  properties:
    pinmux:
      required: true
      type: int
      description: |
        MMIO register and pin bit number

