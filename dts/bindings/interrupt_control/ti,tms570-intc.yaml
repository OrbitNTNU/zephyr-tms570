
description: "Interrupt controller for TMS570"
compatible: "ti,tms570-intc"

include: [base.yaml, interrupt-controller.yaml]

properties:
  reg:
    required: true

interrupt-cells:
  - irq
  - priority
